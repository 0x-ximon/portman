meta {
  name: Create Ticker
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/tickers
  body: json
  auth: inherit
}

body:json {
  {
    "symbol": "ETHSOL",
    "base": "ETH",
    "quote": "SOL",
    "status": "OPEN"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
