meta {
  name: Create Ticker
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/tickers
  body: json
  auth: inherit
}

body:json {
  {
    "symbol": "ADA/USDT",
    "base": "ADA",
    "quote": "USDT",
    "status": "OPEN"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
