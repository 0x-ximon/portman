meta {
  name: Create Sell Order
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/orders
  body: json
  auth: inherit
}

body:json {
  {
    "user_id": "{{USER_ID}}",
    "ticker_symbol": "ETHUSDT",
    "side": "SELL",
    "type": "MARKET",
    "price": 148.85,
    "quantity": 10
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
